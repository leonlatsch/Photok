


package dev.leonlatsch.photok.settings.ui.checkpassword

enum class CheckPasswordState {
    START,
    CHECKING_OLD,
    OLD_VALID,
    OLD_INVALID,
}

package dev.leonlatsch.photok.settings.ui.checkpassword

enum class CheckPasswordState {
    START,
    CHECKING_OLD,
    OLD_VALID,
    OLD_INVALID,
}