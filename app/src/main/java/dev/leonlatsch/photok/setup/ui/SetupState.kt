


package dev.leonlatsch.photok.setup.ui

/**
 * Enum to indicate state in setup.
 *
 * @since 1.0.0
 * @author Leon Latsch
 */
enum class SetupState {
    SETUP,
    LOADING,
    FINISHED
}

package dev.leonlatsch.photok.setup.ui

/**
 * Enum to indicate state in setup.
 *
 * @since 1.0.0
 * @author Leon Latsch
 */
enum class SetupState {
    SETUP,
    LOADING,
    FINISHED
}